---
description: Create and scaffold a new Skill Bridge AI skill
---

# /skill

$ARGUMENTS

---

## Action Required

1. Run the terminal command to generate the skill: `sb skill create "$ARGUMENTS" --index-now`
2. Once complete, inform the user that the skill has been created in the `skills/` directory and is ready for them to edit!
