---
description: Fetch relevant semantic AI context and design tokens using Skill Bridge
---

# /context

$ARGUMENTS

---

## Action Required

1. Run the terminal command: `sb context "$ARGUMENTS"`
2. Verify that `.sb-context.md` was generated successfully.
3. Read `.sb-context.md` completely.
4. Use the injected knowledge from the file to fulfill the user's overarching request.
